---
batch_connect:
  template: vnc
script:
  native: [
          "-N <%= bc_num_slots %>",
          "-n <%= num_cores %>",
          "--partition=<%= sh_queue %>",
          <%- if sh_queue == "gpu" || sh_queue == "gpu-debug" || sh_queue == "gpu-he"  -%>
          "--gres=gpu:<%= num_gpus %>",
          <%- end -%>
          "--mem=<%= sh_mem %>GB",
          <%- if reservation.length >0  -%>
          "--reservation=<%= reservation %>"
          <%- end -%>
          ]
#script:
#  native: ["-w" , "node452"]
